import Head from 'next/head'
import Image from 'next/image'
import styles from '../styles/Home.module.css'
import Button from '../components/Button'
import { AcademyIcon, IdeaIcon,GraphicIcon, PeopleIcon, HatIcon, DownArrow } from '../components/icons'
import Header from '../components/Header'
import { Card } from '../components/Card'
import TestimonialCard from '../components/TestimonialCard'
export default function Home() {
  const list = [{
    text: <><strong>Learn</strong>: mais de <strong>220 módulos</strong>, distribuídos em 7 dimensões empresariais</>,
    icon: AcademyIcon
  },
  {
    text: <><strong>Drive</strong>: transposição do conhecimento para a prática, com <strong>metodologia exclusiva FDC</strong></>,
    icon: IdeaIcon
  },
  {
    text: <><strong>Relatórios gerenciais</strong> para acompanhar o engajamento do time</>,
    icon: GraphicIcon
  },
  {
    text: <>Desenvolvimento <strong>Blended Learning,</strong> com turmas de até 30 pessoas</>,
    icon: PeopleIcon
  },
  {
    text: <>Conteúdos exclusivos, seguindo o padrão de <strong>excelência da FDC</strong></>,
    icon: HatIcon
  }
  ]
  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Header />
      <div>
      <ul className="flex max-md:flex-col flex-wrap justify-center gap-6 max-w-7xl mx-auto">
        <li className="basis-1/3 flex">
          <article className="flex flex-col gap-2 justify-center text-primary-gray-dark">
            <p>Avaliamos as <strong>lacunas do seu negócio</strong> para sugerir os conteúdos que precisam ser trabalhados e isso faz toda a diferença.</p>
            <p>Seu time ainda pode adicionar módulos de interesse de acordo com a cultura e as necessidades pontuais da organização, formando uma <strong> grade curricular flexível.</strong></p>
          </article>
        </li>
        {list.map((item,index) => (
          <li className="basis-1/3" key={index}>
            <Card Icon={item.icon}>{item.text}</Card>
          </li>
        ))}
      </ul>
      <section className="mx-auto max-w-7xl">
        <ol className="list-decimal marker:text-terciary-orange marker:font-bold font-bold" >
          <li>
            <button className="inline-flex items-center justify-between gap-4">
              <h3>Como participar do programa Corporate - Learn & Drive para empresas da FDC?</h3>
              <DownArrow className="w-8"/>
            </button>
          </li>
        </ol>
      </section>
      <TestimonialCard ></TestimonialCard>
      </div>
    </div>
  )
}
